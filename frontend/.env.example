# Frontend environment (copy to .env)
VITE_API_URL=/api/v1
VITE_SOCKET_URL=

# Social sign-in uses backend OAuth routes (/auth/oauth/google, /auth/oauth/apple).
# Configure GOOGLE_* and APPLE_* in backend/.env when ready.
